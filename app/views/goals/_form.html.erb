
    <%= form_for(goal) do |f| %>
      <% if goal.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(goal.errors.count, "error") %> prohibited this goal from being saved:</h2>

          <ul>
          <% goal.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= f.label :title %>
        <%= f.text_field :title, placeholder: "title" %>
      </div>


      <div class="field">
        <%= f.label :description %>
        <%= f.text_area :description, placeholder: "description" %>
      </div>

      <div class="field">
        <%= f.label :target_amount %>
        <%= f.number_field :target_amount, placeholder: "target amount" %>
      </div>

      <div class="field">
        <%= f.label :penalty_amount %>
        <%= f.number_field :penalty_amount, placeholder: "penalty amount" %>
      </div>

      <div class="field">
        <%= f.label :deadline %>
        <%= f.date_field :deadline, placeholder: "deadline" %>
      </div>

      <div class="field">
        <%= f.label :beneficiary_id %>
        <%= f.select :beneficiary_id, Beneficiary.all.collect {|x| [x.name, x.id]} %>
      </div>

      <div class="actions">
        <%= f.submit %>
      </div>
    <% end %>

    <%= link_to 'Back', goals_path %>
